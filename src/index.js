import ReactDOM from 'react-dom';
import App from './App';

var root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />); // append more children to DOM => children: React.ReactNode